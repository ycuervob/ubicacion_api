{
  "name": "localizador-api",
  "version": "0.5",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "test": "nodemon ./bin/www",
    "serve": "npm install && git pull && pm2 start ./bin/www && pm2 monit",
    "restart": "npm install && git pull && pm2 delete www && pm2 start ./bin/www && pm2 monit"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "dotenv": "^16.0.2",
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "http-errors": "~1.6.3",
    "jade": "^0.29.0",
    "morgan": "~1.9.1",
    "nodemon": "^2.0.19",
    "path": "^0.12.7",
    "pg": "^8.9.0",
    "pg-error": "^1.1.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.28.0"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.0"
  }
}
